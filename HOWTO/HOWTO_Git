git remote add origin https://github.com/draskot/Vini

git remote -v

git commit -m "The Vini in silico model of cancer"

git add --all

git commit -m 'First commit'

git remote -v

git push origin master
